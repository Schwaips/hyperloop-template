<% helpers.simple_form_for :form do |f| %>
  <%= f.input :checkbox,
              as: :boolean,
              wrapper: :checkbox,
              label: @label,
              required: @required,
              hint: @hint,
              error: @error,
              disabled: @disabled %>
<% end %>
